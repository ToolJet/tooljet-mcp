{
  "name": "tooljet-mcp",
  "version": "0.2.0",
  "main": "index.js",
  "type": "module",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "dev:http": "tsx src/http.ts",
    "start:http": "node dist/http.js",
    "test": "npm run check:datasource-catalog && vitest run",
    "check:datasource-catalog": "node scripts/datasource-coverage.mjs --check",
    "generate:catalogs": "node scripts/generate-catalog.mjs && node scripts/generate-datasource-catalog.mjs",
    "generate:skill": "node scripts/generate-skill.mjs",
    "sync:skill": "node scripts/sync-skill.mjs --all",
    "build:plugin": "node scripts/build-plugin.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "MCP server and app-builder skill for ToolJet: build and modify ToolJet apps end to end through governed tools.",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@babel/parser": "^7.29.8",
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "bundle",
    "skills",
    "data",
    "README.md",
    "LICENSE"
  ]
}
